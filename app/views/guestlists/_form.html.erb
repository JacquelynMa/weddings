<%= form_for @guestlist do |f| %>

  <% if @guestlist.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@guestlist.errors.count, "error") %> prohibited
        this guestlist from being saved:
      </h2>
      <ul>
        <% @guestlist.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
